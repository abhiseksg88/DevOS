"""
Neural Nexus — The Shared State Engine

The brain that powers the Vedaa agent swarm. Not an agent itself — the
shared knowledge graph that makes every agent smarter with each interaction.

Three dimensions:
  1. User Persona Protocol (UPP) — who the user is
  2. Project State Matrix (PSM) — what the codebase looks like
  3. Business Logic Layer — why the code exists
"""
